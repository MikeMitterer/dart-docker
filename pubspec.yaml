name: docker
description: A Dart wrapper around the Docker command line
author: Mike Mitterer <office@mikemitterer.at>
homepage: https://github.com/MikeMitterer/dart-docker.git

version: 0.2.2

environment:
  sdk: ">=1.8.0 <2.0.0"

dependencies:
  browser: ^0.10.0

  console_log_handler: '>=0.5.0 <1.0.0'
    # path: /Volumes/Daten/DevLocal/DevDart/ConsoleLogHandler

  validate: ^1.0.0

dev_dependencies:
  test: any
  grinder: any